version: "3.9"

services:
  app:
    build: .
    volumes:
      - .:/app
    command: npm run dev
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NEXT_DISABLE_TURBOPACK=1
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true